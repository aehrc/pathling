# minimatch DoS (serve-handler transitive dependency, only used by local
# docusaurus serve command, not in production GitHub Pages deployment).
CVE-2026-26996
CVE-2026-27903
CVE-2026-27904

# serialize-javascript RCE — build-time webpack dependency, not deployed.
GHSA-5c6j-r48x-rmvq

# svgo XML entity expansion DoS — build-time SVG optimiser, only processes trusted source files.
CVE-2026-29074

# follow-redirects header leak — webpack-dev-server transitive, dev-only.
GHSA-r4q5-vmmm-2653

# picomatch ReDoS and method injection — build-time file globbing, not deployed.
CVE-2026-33671
CVE-2026-33672

# brace-expansion DoS — build-time minimatch transitive, not deployed.
CVE-2026-33750

# serialize-javascript DoS — build-time webpack plugin transitive, not deployed.
CVE-2026-34043

# uuid buffer bounds check — mermaid internal use and sockjs dev server only.
GHSA-w5hq-g745-h8pq

# fast-uri path traversal — build-time URI parsing during Docusaurus build, not deployed.
CVE-2026-6321

# postcss XSS via style tag escaping — build-time CSS processing of trusted source files, not deployed.
CVE-2026-41305

# uuid out-of-bounds write — build-time Docusaurus internal use only, not deployed.
CVE-2026-41907

# @babel/plugin-transform-modules-systemjs arbitrary code generation — triggered only by
# malicious source files fed to the build-time Babel transpiler. Not deployed in the static site.
CVE-2026-44728

# fast-uri normalize() percent-encoded authority delimiter issue — fast-uri is pulled in by
# ajv, a build-time JSON schema validator used by Docusaurus tooling. Not deployed.
CVE-2026-6322

# shell-quote command injection — launch-editor transitive, webpack-dev-server
# "open in editor" feature, dev server only, not deployed.
CVE-2026-9277

# ws DoS — webpack-bundle-analyzer transitive, build-time bundle analysis, not deployed.
CVE-2026-48779

# http-proxy-middleware host-header routing bypass — webpack-dev-server dev proxy, not deployed.
CVE-2026-55602

# joi RangeError on deeply nested input — Docusaurus build-time config schema validation
# of trusted config, not deployed.
CVE-2026-48038

# js-yaml merge-key quadratic DoS — gray-matter frontmatter parsing at build time over
# trusted source markdown, not deployed.
CVE-2026-53550

# launch-editor NTLMv2 hash disclosure (Windows UNC) — webpack-dev-server dev server only,
# not deployed.
CVE-2026-53632

# webpack-dev-server HMR WebSocket interception — dev server only, not deployed.
CVE-2026-9595

# brace-expansion out-of-memory DoS — build-time minimatch transitive used for file globbing,
# not deployed. The only fix is a 1.x to 5.x major upgrade of a transitive dependency.
CVE-2026-14257

# brace-expansion DoS via unbounded intermediate arrays — build-time minimatch transitive used for
# file globbing over trusted source paths, not deployed. Fixed in 1.1.18, a transitive dependency
# this project does not pin directly.
CVE-2026-69152

# fast-uri host confusion via backslash in the URI authority — build-time URI parsing during the
# Docusaurus build, not deployed, and not reached by any request-handling path.
CVE-2026-18446

# postcss information disclosure via a crafted sourceMappingURL — build-time CSS processing of
# trusted source files, not deployed.
CVE-2026-69153

# image-size denial of service via crafted image buffers, no fixed release available (2.0.2 is
# the latest published version). image-size is a build-time transitive of @docusaurus/mdx-loader,
# which uses it to measure the dimensions of image files committed to this repository. It never
# sees a request-supplied buffer, and it is not deployed with the static site.
CVE-2025-71329
CVE-2025-71330
