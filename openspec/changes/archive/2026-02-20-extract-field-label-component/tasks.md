## 1. Create FieldLabel component

- [x] 1.1 Write tests for FieldLabel (default rendering, custom mb, optional marker)
- [x] 1.2 Create `FieldLabel.tsx` with props: `children`, `optional`, `mb`

## 2. Replace inline labels with FieldLabel

- [x] 2.1 Replace labels in export components (ExportForm, ExportOptions, ResourceTypePicker)
- [x] 2.2 Replace labels in import components (ImportForm, ImportPnpForm, SaveModeField)
- [x] 2.3 Replace labels in SQL on FHIR component (SqlOnFhirForm)
- [x] 2.4 Replace labels in resources components (ResourceSearchForm, SearchParamsInput)
- [x] 2.5 Replace labels in bulk submit component (BulkSubmitMonitorForm)

## 3. Verify

- [x] 3.1 Run lint, format check, and build
- [x] 3.2 Run existing tests to confirm no regressions
