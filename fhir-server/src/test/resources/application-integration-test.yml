spring:
  profiles:
    active: integration-test
  main:
    banner-mode: off

pathling:
  version: 0.0.0-test

spark:
  master: local[1]
  driver:
    bindAddress: localhost
    host: localhost
  sql:
    shuffle:
      partitions: 1
      autoBroadcastJoinThreshold: -1
  ui:
    enabled: false
