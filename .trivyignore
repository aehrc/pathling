# Netty is a provided dependency, we do not bundle it into our distribution.
GHSA-xpw8-rcwv-8f8p
CVE-2024-29025
CVE-2024-47535
CVE-2025-25193
CVE-2025-24970
CVE-2025-55163
CVE-2025-58057
CVE-2025-58056
CVE-2025-67735

# The vulnerable version of protobuf-java is a transitive provided dependency, we do not bundle it into our distribution.
CVE-2024-7254

# Ivy is a provided dependency, we do not bundle it into our distribution.
CVE-2022-46751

# Zookeeper is a provided dependency, we do not bundle it into our distribution.
CVE-2023-44981
CVE-2024-23944
CVE-2025-58457

# Jersey Client is provided via Spark, not bundled in distribution.
CVE-2025-12383

# LZ4-Java is provided via Spark, not bundled in distribution.
CVE-2025-12183
CVE-2025-66566

# Commons Lang3 is provided via Spark, not bundled in distribution.
CVE-2025-48924

# Log4j is provided via Spark, not bundled in distribution.
CVE-2025-68161

# Nimbus JOSE JWT is provided via Spring Security OAuth2, not directly controlled.
CVE-2025-53864

# OWASP Java HTML Sanitizer XSS - transitive via HAPI FHIR. The vulnerable
# configuration (noscript + allowTextIn("style")) is not used by HAPI's policy.
CVE-2025-66021

# Vert.x static handler cache poisoning - transitive via spark-kubernetes.
# Pathling uses Spring Boot for web serving, not Vert.x static handlers.
CVE-2026-1002

# React Router vulnerabilities - UI uses Declarative Mode (BrowserRouter), no
# SSR, no ScrollRestoration, no loader/action redirects.
CVE-2026-21884
CVE-2026-22029
CVE-2026-22030


# aircompressor Snappy and LZ4 Java-based decompressor 
# io.airlift:aircompressor is a Spark SQL dependency. Snappy is the default compression for parquet/delta.
In the context od the library the the user has full control over compressed content so the attack vector is not applicable.
In the context of the server the bulk import with parquet format could potentially be used as an attack vector. Needs to be investigated further.
CVE-2025-67721
