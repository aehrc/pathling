<?xml version="1.0" encoding="UTF-8"?>
<!--
  ~ Copyright © Australian e-Health Research Centre, CSIRO. All rights reserved.
  -->

<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xmlns="http://maven.apache.org/POM/4.0.0"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>au.csiro.clinsight</groupId>
    <artifactId>clinsight</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <relativePath>../../pom.xml</relativePath>
  </parent>
  <artifactId>notebook</artifactId>
  <packaging>pom</packaging>

  <dependencies>
    <dependency>
      <groupId>au.csiro.clinsight</groupId>
      <artifactId>fhir-server</artifactId>
      <version>${project.version}</version>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-dependency-plugin</artifactId>
        <executions>
          <execution>
            <goals>
              <goal>copy-dependencies</goal>
            </goals>
          </execution>
        </executions>
        <configuration>
          <includeGroupIds>au.csiro.clinsight</includeGroupIds>
          <includeArtifactIds>fhir-server</includeArtifactIds>
        </configuration>
      </plugin>
      <plugin>
        <groupId>com.spotify</groupId>
        <artifactId>dockerfile-maven-plugin</artifactId>
        <executions>
          <execution>
            <goals>
              <goal>build</goal>
              <goal>push</goal>
            </goals>
          </execution>
        </executions>
        <configuration>
          <repository>${dockerRegistry/clinsight/notebook</repository>
          <tag>${project.version}</tag>
          <buildArgs>
            <SPARK_VERSION>${sparkVersion}</SPARK_VERSION>
            <SPARK_SCALA_VERSION>${sparkScalaVersion}</SPARK_SCALA_VERSION>
            <SPARK_MIRROR>${sparkMirror}</SPARK_MIRROR>
            <FHIR_SERVER_JAR>target/dependency/fhir-server-${project.version}.jar</FHIR_SERVER_JAR>
          </buildArgs>
        </configuration>
      </plugin>
    </plugins>
  </build>

</project>
