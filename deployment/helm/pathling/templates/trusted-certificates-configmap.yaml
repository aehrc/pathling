{{- if gt (len .Values.pathling.trustedCertificates) 0 }}
kind: ConfigMap
apiVersion: v1
metadata:
  name: pathling-trusted-certificates
data:
  {{- range $index, $certificate := .Values.pathling.trustedCertificates }}
  ca-{{ $index }}.pem: |
{{ $certificate | indent 4 }}
  {{- end }}
{{- end }}
