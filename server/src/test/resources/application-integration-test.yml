pathling:
  storage:
    databaseName: delta
  import:
    allowableSources:
      - file://
      - http://localhost
      - https://
spring:
  main:
    banner-mode: off

logging:
  level:
    au.csiro.pathling: WARN
